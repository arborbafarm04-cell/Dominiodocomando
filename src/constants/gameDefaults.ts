// Game default constants
const INITIAL_LEVEL = 1;
const INITIAL_MONEY = 100;
const INITIAL_BARRACO_LEVEL = 1;
const SPIN_GAIN_INTERVAL = 5000; // milliseconds
const GRID_WIDTH = 10;
const GRID_HEIGHT = 10;
const TILE_SIZE = 32; // pixels
const MODEL_LOAD_TIMEOUT = 10000; // milliseconds
const CAMERA_FOV = 75; // degrees
const CAMERA_NEAR = 0.1; // meters
const CAMERA_FAR = 1000; // meters
const LIGHTING_INTENSITY = 1.0; // normalized intensity

export {
    INITIAL_LEVEL,
    INITIAL_MONEY,
    INITIAL_BARRACO_LEVEL,
    SPIN_GAIN_INTERVAL,
    GRID_WIDTH,
    GRID_HEIGHT,
    TILE_SIZE,
    MODEL_LOAD_TIMEOUT,
    CAMERA_FOV,
    CAMERA_NEAR,
    CAMERA_FAR,
    LIGHTING_INTENSITY
};